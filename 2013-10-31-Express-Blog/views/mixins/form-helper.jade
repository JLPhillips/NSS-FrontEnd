mixin field(colSize, name, text, placeholder, value, autofocus)
  div(class='small-#{colSize} columns')
    //- label(for=name) #{text}
    input(value=value, type='text', name=name, placeholder=placeholder, autofocus=autofocus)
